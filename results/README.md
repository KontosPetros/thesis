# Results

`artifact_checksums.sha256` freezes the bytes used during the 2026-09-04 audit. Verify from the repository root with:

```bash
sha256sum --check results/artifact_checksums.sha256
```

Legacy artifacts remain at the repository root until the next cleanup commit preserves and updates all references.

A detailed machine-readable experiment registry has been prepared, but is not part of this first public commit pending explicit confirmation of what non-public evidence may be published.
